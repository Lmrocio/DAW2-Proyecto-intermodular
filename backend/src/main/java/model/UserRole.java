package model;

/**
 * Enum que define los roles disponibles en la plataforma.
 * USER: Usuario regular con acceso limitado a consulta y funcionalidades de perfil.
 * ADMIN: Administrador con permisos para crear, editar y eliminar contenido.
 */
public enum UserRole {
    USER,
    ADMIN
}

