package model;

/**
 * Enum que define los tipos de entidades que pueden ser auditadas en la plataforma.
 */
public enum AuditEntityType {
    USER,
    LESSON,
    STEP,
    SIMULATOR,
    FAQ,
    CATEGORY,
    USER_LESSON_PROGRESS,
    USER_SIMULATOR_INTERACTION
}

